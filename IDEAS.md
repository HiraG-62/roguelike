# ブラウザローグライク 企画メモ（引き継ぎ用）

作成日: 2026-09-23
目的: 趣味開発。「機能を足し続けて遊べる」ことを最優先にする。

## コンセプト

- ブラウザで動くターン制ローグライク（Rogue / NetHack / DCSS 系）
- 1プレイ 15〜30 分、死んだら最初から（パーマデス）、シードで再現可能
- 見た目は ASCII を基本にし、後からタイル描画へ切替できる構造にする

## 技術スタック（決定事項）

- TypeScript + Vite、ランタイム依存ライブラリなし（描画は Canvas 2D）
- テストは Vitest。生成アルゴリズムと戦闘計算はロジックを純関数化してテストする
- 状態は「ゲームステート（純データ）」と「描画」を完全分離する
  - 理由: セーブ/リプレイ/シード再現/テストが全部この分離に依存するため
- 乱数は自前の seedable PRNG（mulberry32 か xoshiro）。`Math.random` は使わない
- ディレクトリ案
  ```
  src/
    core/      ゲームループ、ターン管理、イベントバス
    map/       ダンジョン生成、FOV、経路探索
    entity/    プレイヤー、モンスター、アイテム定義
    system/    戦闘、状態異常、AI、インベントリ
    render/    Canvas 描画、ASCII/タイル切替
    ui/        ログ、メニュー、インベントリ画面
    data/      モンスター/アイテムのデータ（JSON or TS）
  ```

## ロードマップ（各フェーズは「遊べる状態」で終える）

### Phase 0: 骨組み（目標 1〜2 時間）
- Vite プロジェクト作成、Canvas に 80x24 グリッドを描画
- 部屋をランダム配置して通路でつなぐ（BSP or 単純な部屋＋L字通路）
- `@` を矢印キー / hjkl で移動、壁で止まる
- 階段 `>` で次の階へ（フロア番号だけ増える）

### Phase 1: ローグライクとして成立させる
- Shadowcasting FOV（視界外は暗転、探索済みは記憶表示）
- モンスター配置、bump 攻撃、HP、死亡でゲームオーバー
- メッセージログ（下部 or 右側）
- 経験値とレベルアップ
- シード表示と入力（同じシードで同じダンジョン）

### Phase 2: 深みを出す
- アイテム: 拾う/落とす/使う。ポーション、巻物、武器防具
- 未鑑定システム（「赤いポーション」→ 飲むと正体判明、シードごとにランダム対応）
- 敵 AI: Dijkstra マップで追跡、HP 低下で逃走、群れで包囲
- 状態異常: 毒、麻痺、混乱、燃焼。相互作用（油＋火＝爆発、水＋雷＝感電）
- 複数の生成アルゴリズムをフロア種別で切替
  - BSP（通常階）、セルオートマトン（洞窟階）、迷路（迷宮階）、固定 vault（ボス階）
- ローカルセーブ（localStorage）、実績、死亡時の「墓標」画面

### Phase 3: 好きな順に伸ばす（バックログ）
- デイリーチャレンジ（日付シード）、スコアランキング
- リプレイ記録（シード＋入力列だけ保存すれば再生できる）
- 光源システム（松明の範囲、暗闇階、光る敵）
- 演出: パーティクル、画面揺れ、ダメージ数字ポップ、ASCII ↔ タイル切替
- スキルツリー / クラス（戦士・盗賊・魔法使い）
- クラフト・装備合成、呪われた装備
- 店・NPC・クエスト
- 音（Web Audio API で効果音を合成、BGM はループ素材）
- Claude API 連携: フロア到達時のフレーバーテキスト、NPC 会話、墓標の死因文を生成
- モバイル対応（タップ移動、スワイプ）
- Mod 対応: data/ を JSON 化して外部から差し替え可能にする

## 設計上のこだわり（後で自分が困らないために）

- 全アクションは「イベント」として発行し、ログ・実績・リプレイはイベント購読で実装する
- モンスターとプレイヤーは同じ Entity 型。プレイヤーは「入力で行動を決める Entity」でしかない
- ターン制はエネルギー方式（速度差を表現しやすい。DCSS 方式）
- マップは 1 次元配列 + インデックス計算（2 次元配列より速く、シリアライズも楽）
- 画面サイズは論理グリッド固定、Canvas の拡大縮小で対応する

## 最初のセッションでやること

1. `C:\Users\Horry\dev\roguelike` で `npm create vite@latest . -- --template vanilla-ts`
2. Phase 0 を一気に作る（設計は上記に従う）
3. 動いたら `git init` して `feat: ダンジョン生成と移動の骨組み` でコミット
4. 以降は Phase 1 を 1 機能 = 1 コミットで進める

## 参考にするもの

- RogueBasin（アルゴリズム解説の定番）: https://www.roguebasin.com/
- Red Blob Games（FOV, 経路探索の図解）: https://www.redblobgames.com/
- 「Roguelike Tutorial in Rust / Python」の構成（章立てを参考にする）
