# エフェクトのスプライト化（高精細ドット絵・モーション専用）

作成日: 2026-09-25
状態: 基盤と近接 18 武器種（剣 + 17 種）を実装。銃・杖・技・スキルは同じ手順で順に足す

## 1. 目的と決めたこと（ユーザーの判断）

- 攻撃・スキル・パーティクルなど、エフェクトをすべて手続きの図形から **専用のドット絵スプライト** に置き換える。汎用の使い回しで済ませず、武器・モーション・スキルごとに作り込む
- **描画の密度は 2 倍（960x540 相当）**。ゲームの座標（当たり判定・距離・ロジック・リプレイ）は 480x270 のまま変えない
- キャラ・タイル・UI の描き直しは **段階的に** 進める。途中はエフェクトだけ 2 倍の密度になり、ドットの粗さが混ざる
- 絵は **生成スクリプトで PNG に焼く**（`scripts/fx/`）。形・動き・色の段をコードで組み、1 枚ずつ目で確かめて詰める
- 進め方は **1 武器で縦に作り切る**（剣）。基準が固まってから横に広げる

## 2. 密度 2 倍の仕組み

- canvas の実ピクセルは、すでに「論理 1px = `pixelRatio` 実 px」（`renderMath.ts` の `computeViewScale`）。ウィンドウが 960x540 以上なら `pixelRatio` ≥ 2 になる
- エフェクトの絵は **絵の 1 ドット = 論理 0.5px**（`FX_ART_SCALE = 2`）。`drawImage` の描き先の大きさを絵の半分にして置くだけで、全体の作り替えは要らない
- 置く位置は 0.5px 単位に丸める（`snapHalf`）。絵のドットが実ピクセルの格子に乗る
- `pixelRatio` < 2 の小さなウィンドウでは、絵は最近傍で縮んで粗くなる（許容）

## 3. 生成器（`scripts/fx/`）

依存なし（Node 標準の `zlib` だけ）。生成物はコミットし、ビルド時には生成しない。**1 武器種 = 1 ファイル**で完結させ、武器種ごとに並列で作れるようにしてある（共有ファイルへの登録は要らない）。

| ファイル | 役割 |
| --- | --- |
| `png.mjs` | RGBA の PNG を書き出す・寸法を読む |
| `raster.mjs` | 1 枚の絵（段の番号の格子）を作る道具: 超標本化の塗り・段の量子化・順序ディザ・孤立ドットの掃除・座標ハッシュのばらつき |
| `shapes.mjs` | 形の部品: 三日月・レンズ形の斬線・弧 / 直線の速度線・輪・刃片・光点 |
| `motifs.mjs` | 複数の武器で使う時間割: 弧の斬撃（`arcSlash`）と、そのシートの作り方（`arcSheet`）。方向の数 `DIRS` |
| `pack.mjs` | 余白を切り詰めた全フレームを棚詰めで 1 枚に並べる |
| `sheets/<武器種>.mjs` | 武器種ごとのモーションの絵と、モーション → シートの表（`export const ATLAS = { key, sheets, fx }`。ファイル名 = key） |
| `gen.mjs` | `sheets/` を自動で集めて描き、`public/assets/fx/<key>.png` と `src/data/fx/<key>.gen.json`（寸法・矩形・表）、束ねる `src/data/fxSheets.gen.ts` を書く。`--atlas <key>` でその武器種だけ、`--only <シート> --preview <dir>` で確認用 PNG だけ、`--check` で最新か確かめる |

### 3.1 色は「段」で持ち、配色は実行時に差し替える

- PNG には色ではなく **段の番号**（1〜7。暗 → 明）を灰色で書く（R = G = B = 段 × 32）
- 実行時に **配色（ramp）** で段を色へ写す（`render/fxRamps.ts`）。既定の鋼（物理）と属性 6 種の 7 段の配色を持つ。属性が乗った剣は、同じ絵が炎や氷の色になる
- 配色はシートと配色の組ごとに初回だけ作ってキャッシュする（毎フレームの合成はしない）
- 段の使い方: 7 = 白い芯、6〜5 = 明部、4〜3 = 本体、2 = 縁の暗部、1 = 最暗の散り（煤・影）

### 3.2 方向は事前に描く（回転しない）

- ドット絵を任意角で回すとジャギーが崩れるので、**方向ごとに生成器で描き直す**（数式で描くので、どの角度でもドットが整う）
- 既定は 24 方向（15° 刻み、誤差 ±7.5°）。向きの無い絵（受け流しの閃光・巴）は 1 方向
- **振りの左右（時計回り / 反時計回り）は描き分けない**。反時計回りの角 θ の絵 = 時計回りの角 −θ の絵の上下反転。24 方向は −θ でも同じ組に乗るので、反転してもドットがずれない

### 3.3 詰め込みと原点

- 各フレームは不透明なドットの外接矩形に切り詰め、原点（pivot。絵の中の「振りの中心」や「当たり判定の中心」）からのずれを一覧に持つ
- 一覧（`src/data/fx/<key>.gen.json` を `fxSheets.gen.ts` が束ねる）: シートごとに `atlas`・`frames`（フレーム数）・`dirs`（方向数）・`active`・矩形の表 `[x, y, w, h, ox, oy]`（`dir * frames + frame` の順）と、武器種のモーションの表 `fx`
- テスト（`render/fxSprites.test.ts`）が一覧と PNG の寸法・矩形のはみ出し・件数と、表のある武器種が振りのモーション（左の段・ダッシュ・右の振り・派生・溜め）をすべて持つことを検査する

## 4. 実行時（`render/fxSprites.ts`）

- 装備中の武器種のアトラスだけを読み込み（`FxSpriteBank.focus`。失敗・未読み込みは今までの手続きの描画にフォールバック）、配色ごとのフレームを初めて描くときに作る
- `FxSpriteBank.draw(ctx, sheet, frame, x, y, angle, opts)`: 角を最寄りの方向に丸め、反時計回りは上下反転で引き、論理座標の 0.5px 格子に置く
- 反転（`FxMotion.mirror`）: 既定の `swing` は反時計回りの段で上下反転する。突きのように手に持つ武器の刃・鉤の側が向いている左右で入れ替わるモーションは `faceLeft` / `faceRight`（左 / 右を向いて出したときに反転）にして、非対称な絵（大鎌の鎌引きの鉤・鞭の巻き付けの螺旋）を武器の向きに合わせる
- 地面の層（`FxMotion.ground`）: 地割れ・地面の輪・砂煙などキャラより下に描く絵を別のシート（`<key>.ground`）にし、空中の絵と同じフレームで流す（戦鎚の叩きつけ）。描くのは `drawGroundHazards` の中（キャラより先）
- 押している間の絵（`MovesetFx.holds`）: 右の溜めの段が回し（`spinning`）を持つとき、押している間ずっと繰り返す絵（チェーンアレイの右長押し）。`spinning.interval` で 1 周し、溜めの段が 1 以上なら `charged` の絵
- 振りの残像の線（`player.ts` の `spawnTrail` が引く `ShapeFx.swingTrail`）は、専用スプライトが読めている武器種では描かない（手続きの線が重なって二重に見えるため）
- モーションの key（`fxMotions.ts` の `motionKey`）: ダッシュ攻撃 `dash` → 派生 `branch:<key>` → 溜め攻撃 `charge` → 右の段 `r:<key>` → 左の段 `l:<段>`（`player.ts` の `stepDef` と同じ順）。右の段の構え・投げ・溜め、杖の詠唱は対象外（手続きの描画）
- 時刻は `state.time` から数える（ヒットストップで止まる）。`state` は読むだけ、`state.rng` も使わない（不変条件 1〜3）
- 当たり判定の大きさ（`meleeReachMul` などで伸びる）と絵の基準の大きさが違うときは、描き先を比で拡縮する（基準から ±15% 以内なら拡縮しない＝ドットを崩さない）

## 5. 剣のモーション一覧（縦切りの範囲）

| モーション | 当たり判定 | シート | 原点 | フレーム | 絵の狙い |
| --- | --- | --- | --- | --- | --- |
| 左 1 段 | box reach 16 / size 26 | `sword.l1` | 自分 | 8 | 右から左へ走る細めの三日月。先端に光点、尾は速度線にほどける |
| 左 2 段 | box reach 18 / size 28 | `sword.l2` | 自分 | 8 | 1 段目の返し（逆回り）。わずかに低い軌道・長い尾 |
| 左 3 段（終撃） | box reach 22 / size 38 | `sword.l3` | 自分 | 9 | 1 本の太い三日月 + 飛び散る刃片。最大の明るさ（内側に 2 本目の弧を重ねると二重に見えるので重ねない） |
| ダッシュ攻撃 | box reach 26 / size 30 | `sword.dash` | 自分 | 8 | 前へ流れる横一文字の斬撃と、後ろへ伸びる速度線の束 |
| 右: 返し斬り | thrust reach 30 | `sword.thrust` | 自分 | 7 | 鋭い刺突の光条 + 穂先の衝撃の輪 2 枚 + 平行の速度線 |
| 右: 斬り上げ | arc 150° reach 26 | `sword.rising` | 自分 | 9 | 下から上へ大きく跳ね上がる三日月。頂点で刃片が上へ散る |
| 派生: 十字断ち | box reach 22 / size 40 | `sword.cross` | 当たりの中心 | 9 | 2 本の斬線が時間差で交差し、交点が閃光で弾ける |
| 派生: 踏み込み斬り | thrust reach 30 | `sword.step` | 自分 | 7 | 長い踏み込みの一閃。刺突より細く長く、後ろに残像の線 |
| 派生: 巴 | circle size 44 | `sword.tomoe` | 当たりの中心 | 10 | 2 つの巴（勾玉形）の渦が 1 周する。2 段ヒットに合わせて 2 回明滅 |
| 派生: 逆袈裟 | box reach 22 / size 40 | `sword.kesa` | 当たりの中心 | 8 | 左下から右上へ抜ける太い一本の斜線。断面から火花 |
| 命中（軽） | — | `sword.hit` | 敵 | 6 | 刃の進む向きの鋭い裂け目 + 十字の閃き + 刃片 |
| 命中（重） | — | `sword.hitHeavy` | 敵 | 7 | 裂け目が太く、周囲に衝撃の輪と大きな刃片 |
| 受け流し成功 | — | `sword.parry` | 自分 | 8 | 金属の火花の放射 + 八方の閃き + 輪 |

- 絵の大きさの目安（絵のドット）: 左 1・2 段 ≈ 110x100、終撃・斬り上げ ≈ 150x130、十字・逆袈裟・巴 ≈ 100〜128、命中 ≈ 48〜72、受け流し ≈ 96
- 振りの時間は短い（active 0.08〜0.1 秒 = 5〜6 描画フレーム）。フレームは **振りの進み（active）に前半、振り終わりの尾（`FX_ATTACK.slash.fadeTime`）に後半** を割り当てる（`fxTiming`）

## 6. 近接 17 武器種（2026-09-25 に並列で制作）

剣を基準に、1 武器種 = 1 エージェントで並列に作った（手順書は各武器種のファイルの冒頭と `docs/recipes/sprite.md`）。武器種ごとの絵の軸:

| 武器種 | 絵の軸（剣と見分ける所） |
| --- | --- |
| 大剣 greatsword | 先の鈍い分厚い帯（刃の腹が最も太い）、床の砂煙と角ばった石礫、叩きつけの楕円の輪と亀裂 |
| 双剣 twinBlades | 細く短い斬線を角度とタイミングをずらして連ねる、影の残像、二刀の交差の命中 |
| 槍 spear | 細い柄の光と菱形の穂先、穂先の前の V 字・潰れた輪、根元から崩れて穂先が残る |
| 大鎌 scythe | 先端が内へ J 字に巻く細い鎌の刃、煙のように千切れる尾、刃の外へ漂う煙と魂の粒 |
| 拳 fists | 刃を使わない。不揃いな衝撃の星・潰れた輪・風圧の筋、蹴りは縁の白くない厚い空気の帯 |
| 鞭 whip | 先端へ伝わる S 字の波と先端の破裂、薙ぎは本体と帯を 1 本の線でつなぐ |
| 鉈 cleaver | 外縁が折れ線の角ばった楔、四角い破片・四角いブロックの崩れ、八角形の衝撃の枠 |
| 棍 staff | 刃の白い縁を使わない。放射状の細い筋の扇（棒のブレ）と先端の丸い打撃 |
| 刀 katana | 極細の一閃のあとに遅れて光る切断線、粒は少なく、峰打ちだけ白を使わない鈍い打撃 |
| 斧 axe | 斧頭側がどっしり太い楔の弧、棒状の木片、V 字の切り込み |
| 盾 shield | 前へふくらむ平たい板（前縁だけ白）、両端から横へ開く衝撃の弧と火花 |
| 鎖鎌 chainSickle | 小さく鋭い鎌の三日月と、交互の輪で描く鎖・先端の分銅 |
| 戦鎚 hammer | 刃を使わない。画面に揃えた地面の楕円の輪・枝分かれの地割れ・石の破片・砂煙 |
| 爪 claws | 3 本の平行な爪痕（爪の個性として例外的に複数線）、牙の噛み合わせ |
| チェーンアレイ flail | トゲ付きの鉄球と通り道の残像、手元からの鎖、叩きつけは落ち方で描き分け |
| チャクラム ringBlades | 外周にのこぎり歯のある細い帯と、歯付きの転がる輪、接線へ飛ぶ火花 |
| 扇子 fan | 扇の骨の放射線・外周の風の帯・菱形の花びら、回転は螺旋の風と周回する花びら |

- **振り幅 180° 以上の弧と 1 周する振りは 12 方向**（`motifs.mjs` の `WIDE_DIRS` / `WIDE_SWEEP`）。面積が大きく、24 方向だとアトラスが 100MB 近くなったため。回り込む弧は 30° 刻みでも向きの誤差が目立たない
- 地面に広がる物（砂煙・楕円の輪）は画面に揃えて描き、上下対称に近くする（反時計回りの段は上下反転されるので、上下に偏ると反転で崩れる）
- 対象外（手続きの描画のまま）: 右の段の構え・投げ・溜め（受け流し・盾の構え・刀の居合の構え・投擲など）、ジョブで足される派生（杖の魔法・銃の家系は 9 章で専用の絵にした）

## 7. 次に広げる順

1. ~~銃の家系 8 種と杖の魔法、奥義~~（9 章。2026-09-26 に全武器種・奥義 81 本を制作）
2. 武器技（298）・共通技（24）。技ごとの専用シート
3. スキル石・刻印符（約 100）
4. 状態異常・状態の反応・敵の攻撃・死亡演出・パーティクルの置き換え
5. キャラ・タイル・UI の 2 倍密度での描き直し（別レーン）

## 8. 分かっている代償

- メモリ: 1 武器種のアトラスは展開後およそ 40MB。実行時は装備中の武器種のアトラスだけを読み、持ち替えたら前の分を捨てる（`FxSpriteBank.focus`）。持ち替えた直後の数フレームは読み込み中で、今までの手続きの描画になる
- 当たり判定が大きく伸びた装備（±15% 超）では絵を拡縮するので、ドットがわずかに崩れる。必要になったら大きさの段（大・特大）を生成する
- 描き直すまでは、キャラ（24px）とエフェクト（2 倍密度）でドットの粗さが違う

## 9. 弾・魔法・奥義（2026-09-26）

ユーザーの依頼で、銃の家系 8 種・杖・奥義（27 武器種 × 3 本）を同じ作りで専用の絵にした。見本（片手銃・杖・剣の奥義）をユーザーが確認したあと、残りを並列で制作。全武器種が絵の表を持ち、全奥義が発動と行為（持続は纏い）の絵を持つことは `render/fxSprites.test.ts` が固定する。

- 投げた武器（`render/thrownLook.ts` の表に載る弾・奥義。投げナイフ・輪・斧など）は武器そのものの絵が本体で、弾の `fly` はその下に重なる風切りの軌跡・残像・光だけを描く（本体を描くと二重になる）
- 近接の武器種が右の段で撃つ弾（斧の投擲・扇子・チャクラムの投げ）は、まだ弾の表を持たない（投げた武器の絵と手続きの尾で描く）

### 9.1 弾（銃・杖・弾を出す技）

- 表は武器種のアトラスの `fx.bullets`（弾の key → 絵）。弾の key は `BulletDef.key`（銃のベースの key・`art.<右の段>`・`cast.<cast>`）。弾を出す武器種の表は、その武器種が撃つ弾をすべて持つ（テスト）
- 1 弾 = `fly`（飛んでいる間 `period` 秒で繰り返す。尾も絵が持つ。向きは速度の向き）・`muzzle`（撃った瞬間。杖は詠唱の紋）・`impact`（壁・敵で消えた）・`hit`（敵に当たった。貫通しても出る）・`fizzle`（射程で尽きた）・`blast`（設置弾・曲射の炸裂。`blastBase` は描いたときの半径）。`base` は描いたときの弾の半径で、今の半径と 15% 超違えば拡縮する
- 配色は弾そのものの属性（魔法）→ 射撃の属性（装備）→ 表の `ramp`（物理の弾は真鍮 `brass`。曳光の色）
- 実行時は `render/fxShots.ts`。敵への命中は「前の tick にその敵の近くにいた自分の弾」を当てた弾とみなして `hit` を出し、同じ tick に消えても `impact` を重ねない。炸裂は `system/projectiles.ts` が爆発の輪に弾を結ぶ（`markBlastShot`、弱参照）
- 銃の武器種も近接の振り（銃床・銃剣・蹴り・ダッシュ攻撃・派生）は近接と同じ `motions` に載せる

### 9.2 奥義

- 奥義のアトラスは武器種ごとに `<武器種>Ult`（`scripts/fx/sheets/<武器種>Ult.mjs`。3 本をまとめる）。実行時は装備の武器種のアトラスと、選んでいる奥義のアトラスの 2 つを持つ（`FxSpriteBank.focus`）
- 奥義は発動の瞬間に行為を出し切るので、描画が拾えるよう `system/ultimates.ts` が見た目の出来事を積む（`EffectsState.ults`、`addUltFx`）。種類は cast（発動）・act（一撃の行為。番号は行為の並び）・target（引き寄せで掴んだ敵ごと）・aura（持続の纏いの 1 回）・quake（持続の命中の衝撃波）・end（持続の終わりの行為）。大きさ（周囲攻撃の半径・振りの届き・突進の距離）と向き・始点と終点を持つ
- 表は `fx.ultimates`（奥義の key → `{ ramp, cast, acts[], target, ends[], aura, quake, sustain, shots }`）。1 つの絵は `{ sheet, life, base, pivot: pos|to, mirror?, ground? }`。`sustain` は持続中に自分の周りで繰り返す絵、`shots` は行為の番号 → その行為が出した弾の絵（`withUltimateFx` が弾に奥義と番号を結ぶ）
- 奥義が出した輪・線・粒には印が付き（`withUltimateFx`）、絵が読めていれば手続きの描画を省く。奥義名の浮き文字は残す
- 配色は奥義の素性の属性、無ければ表の `ramp`（既定は光）

